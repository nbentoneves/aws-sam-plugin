package com.github.sam.debug

class SamDebugExtension {
    /**
     * Directory (relative to the project root) where run-sam-debug.sh will be copied.
     * Defaults to ".aws-sam".
     */
    String outputDir = '.aws-sam'
}
